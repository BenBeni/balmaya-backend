# Replace with real domains before production deploy.
http://89.167.95.134 {
    encode zstd gzip
    reverse_proxy app:8081
}

# Optional: expose Keycloak admin and OIDC endpoints via HTTPS if required.
http://89.167.95.134:8080 {
    encode zstd gzip
    reverse_proxy keycloak:8080
}
